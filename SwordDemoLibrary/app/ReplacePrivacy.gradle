def projectPrivacyFiles = [
        'game_channel_user_privacy.txt',
        'game_channel_user_services.txt',
        'game_privacy_policy_logo.png',
        'game_channel_user_children_privacy.txt'
]

def originPrivacyFiles = [
        '决铭-儿童隐私政策.docx',
        '决铭-隐私政策.docx',
        '决铭-游戏许可及服务协议.docx'
]

def originPrivacyPath = "\\\\10.19.8.64\\迅龙创威资源共享盘\\个人目录\\swordzhou\\隐私协议"

def replaceChannels = ['Oppo', 'Vivo', 'Huawei', 'Xiaomi', 'Yingyongbao', 'NineGame', 'Quick', 'Xiaoqi']

tasks.register("replacePravicy") {
    //读取原文件，重命名为需要的格式以及文件
    def tempDirs = new File("$originPrivacyPath${File.separator}tempDirs")
    if (!tempDirs.exists()) {
        tempDirs.mkdirs()
    }

    originPrivacyFiles.forEach { fileName ->
        def sourceFile = new File("$originPrivacyPath${File.separator}$fileName")
        println "sourceFileName: ${sourceFile.getAbsolutePath()}"
        
        if (fileName.endsWith("docx")) {
            converDocxToTxt(sourceFile)
        }
    }
}



