<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sword.customviewset">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomViewSet">
        <activity android:name=".ViewDrawAnalyse"></activity>

        <activity android:name=".LayoutLayout"></activity>

        <activity android:name=".LayoutSizeActivity"></activity>

        <activity android:name=".MainActivity" ></activity>

        <activity android:name=".AnimatorActivity" />

        <activity android:name=".ScaleImageViewActivity">
        </activity>

        <activity android:name=".multitouchview.MultiTouchViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>